#ifndef QSORT_CAPSULE_H_
#define QSORT_CAPSULE_H_

#include "array.h"

void qsort_param_init();
void do_capsule(struct array_s *array);
void do_capsule_grouped(struct array_s *array);
void do_seq(struct array_s *array);

#endif
