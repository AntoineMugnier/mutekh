(dans l'ordre)

* alignements memoire dans rarp et dhclient

* libsocket
  - finir sockets UDP

* route
  - gestion par objets refcount
  - faire une version light avec liste triée

* if
  - a coder integralement
  - gerer les trucs fun genre bridging

* lwNFS
  - adapter la nouvelle API UDP

* libsocket
  - sockopt sur socket IP

* TCP
  - a finir

* libsocket
  - sockets TCP

* route
  - faire avec un arbre comme a dit Alex

* filter
  - a coder integralement

* ARP
  - gestion des doublons

* IP
  - idee de nipo: dans le routage avec fragmentation, calculer taille optimale

* general
  - verifier l'alignement des acces memoire
  - voir les locks un peu partout
  - prevoir une gestion d'autre chose qu'Ethernet ?
